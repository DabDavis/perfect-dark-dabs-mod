# Perfect Dark port — simulant/mod fork

Fork of the [fgsfdsfgs/perfect_dark](https://github.com/fgsfdsfgs/perfect_dark) port.
`port` tracks upstream; work happens on `dabs-mod`.

`git checkout port` returns to stock at any time.

## What is written down here

Each note in `CLAUDE-notes/` is a thing that was got wrong once. Read the note
for an area **before** touching it — none of them are inferable from the code,
and each one cost a detour. This file keeps only what every session needs; the
notes are read when their area comes up.

- **The Windows build, wine, the pd.ini format** — [windows-build.md](CLAUDE-notes/windows-build.md): mingw prefix, WinHTTP, and why `Mod.LoadTextures=1` on its own line does nothing
- **chrs, bodies, heads, simulants, memory pools, mpconfig** — [chrs-and-memory.md](CLAUDE-notes/chrs-and-memory.md): a chr's prop is read before its tick; the ~50KB head copy that empties the stage pool; one head modeldef cannot sit on two bodies; ROM-resident structures never grow; why kept bodies still vanished (the chr vertex store's reaper, sized for an N64, fades every off-screen corpse when the blood copies run out)
- **A head on a body it was not made for** — chrs-and-memory.md, "Heads fitted to bodies by measurement": `port/src/headfit.c` replaces the ROM's type table for mixed pairs (own heads and stock one-type pairs untouched); a list's vertices are in the space of the G_MTX loaded before them, not the node's; the release's meshes now take a head's offset through `xblaMeshPose()`
- **Saves, eeprom, where pd.ini lives, the migration** — [save-format.md](CLAUDE-notes/save-format.md)
- **Menu text, `textMeasure()`, reaching the widescreen pillars** — [text-rendering.md](CLAUDE-notes/text-rendering.md)
- **Adding stages** — [stage-numbers.md](CLAUDE-notes/stage-numbers.md): `STAGE_IS_LEVEL()` admits 0x5e-0xff as well as the 27 free below the title; four ids are taken outside the table; the MP save format holds 7 bits
- **The Stage Loader: every mod's maps as arenas beside the mod loaded** — mods.md, "The Stage Loader": maps-only mounts never overlay; the registrar rescans on a swap; the branch's fixes (allocation, pool checks, room sizing, textures by stage); the importer splits a rebuilt texture table (30) and writes the `maps` block (31) so a mod's arenas come from its own tables under its own names — a file name is Perfect Dark's slot, not the map (GE-X's `crad` is Aztec)
- **Screenshots, the recorder, ffmpeg, GL capture** — [recording.md](CLAUDE-notes/recording.md): the frame is presented before `videoEndFrame()`; NV12 on the GPU; encoder detection; why it must never wait for the encoder; running on the real GPU with no window (llvmpipe hides driver limits); **the F3 trace dump** (`Mod.TraceKey`, port/src/trace.c) (walks live props only: a freed prop slot's stale chr crashed F3 on Chicago): a text dump of the pools, the loaders, the renderer's cache, the camera and every chr with what chrRender() did with it, paired with a screenshot - what to ask a tester to press when something is missing from a frame; since 2026-09-17 F3 also opens **Report a Problem** (`port/src/tracereport.c`, `Mod.TraceReport`): a typed note, and Send posts the dump, settings, log ring and a scaled PNG to pdghostd's `/report` (`~/pdghosts/reports`, nginx body limit must be 8m) - opened from `lvTick()`, and typing must stay on through the send or the held ENTER closes the dialog (recording.md, "Report a Problem")
- **GE Plus's level music** — ge-bean.md, "The levels' own music": GoldenEye's `music_setup_entries` row on each map/mission line (converter 45), `gemusic.c` appends the ROM's sequences on one shared bank with GoldenEye's own volumes; a converted mission was silent because Perfect Dark starts a mission's music from its intro's AI list; background plays level-wide; watch and death tunes
- **Music a mod or a conversion adds** — ge-bean.md, "Borrowed music read its volume past the end of a table": `seqSetVolume()` scales a sequence by `var8005ecf8[tracknum]`, which has 121 rows for the game's 119 sequences, so every sequence `seqAppend()` hands out (GE-X's borrowed music, GoldenEye's folder and intro themes) read past it - one drew `AL_VOL_FULL` and played three times as loud. They take Perfect Dark's own menu scale now. `SDL_AUDIODRIVER=disk` + `SDL_DISKAUDIOFILE` is the quickest way to hear a headless run
- **Ghost Trials networking** — [ghost-trials.md](CLAUDE-notes/ghost-trials.md): WinHTTP and libcurl, why not one of them, and what the worker thread may touch; the three security questions' wire format and `rec_count`; the red Streamer Beware door and the Offline/Online page; **the pd.ini key is `Mod.GhostServer`** - point a scratch ini at a local daemon before any headless drive that presses Create Account
- **Crash reports** — [crash-reports.md](CLAUDE-notes/crash-reports.md): `sysFatalError()` writes one before the dialog goes up (build, mod, the `[Mod]` settings live rather than from the file, and the last 200 log lines from a **ring** in crashreport.c, because pd.log only exists under `--log`); the dialog's *Send report to Dab* button and *Crash Reports* on the Perfect Menu are the only two things that send one, and the note is typed on the page because a message box cannot take text; `POST /crash` on pdghostd stores it as a file nothing serves back; a setup error the player fixes from the message (no ROM, wrong ROM, no OpenGL 2.1) is `sysFatalSetupError()` and writes none; a report carries `[Game]` and the pools' free space too; and **reading them** - scp from `~/pdghosts/crashes`, symbolise against the release's own binaries, group by first-frame offset - is in the note
- **Check for Updates** — [updater.md](CLAUDE-notes/updater.md): `update.txt`, the baked-in channel, the two-rename swap
- **Mod directories, Load Mods, modconfig, `modcodediff`, the ROM symbol file, the data segment and importing a mod's weapon definitions** — [mods.md](CLAUDE-notes/mods.md): only the first mod dir joins the file search; files swap live, segments cannot; the `datasegment` block, `moddata.c`, and "where this stands" for continuing the import work
- **Texture packs** — [texture-packs.md](CLAUDE-notes/texture-packs.md): where a pack goes (one of four directories is read by nothing); decoding off the render thread and the backlog; the kept store and why a decode must never be handed over (textures flicker to the original otherwise); an emulator pack's image is the tile, the renderer maps the padded row; the *other* stretch is the game's own - a tile sampled past its edge, which no image edit can fix and **Stretched Edges** (`Video.StretchedEdges`: Original/Mirror/Repeat) can; PNG is ours, JPEG is stb_image, and the two row orders; font glyphs (the image is the whole tile, the outline pass wants both images); F7–F10; a folder named **`xbla`** inside a pack replaces the XBLA release's own texture records, by record rather than by number, on its models *and* its rooms (F7 writes them out in the same layout, which is the only way to learn which record a surface is); and **Community Packs**, which installs a pack from its author's release page - what is in the binary is the pack and not the release, the row-order marker is written at install time because v0.09 of the PD Plus pack renamed the `ext_tex` folder that used to say so, and the cover art is drawn through a stand-in tile the way an XBLA mesh's texture is (and is *not* turned over); since 2026-09-15 it is three packs from the one v0.10 release (Ultimate, XBLA and Forever Plus HD) on three swipe pages, one ask answering all three, every row asking about *its* pack (item `param`) because every sibling page is drawn, and **v0.10 is stored the right way up**, so no marker - the opposite of v0.09; and **a texture number belongs to whatever supplied the texels** (2026-09-12) - a mod's map loaded by the Stage Loader draws its own art at stock numbers (GE-X: 2104 of them, all below `NUM_TEXTURES`), so a picture chosen by number is the wrong subject *and*, since a pack replaces pixels and not the tile, stretched over it - `texpackTextureArt()` tells the three sources apart, a maps-only mod's texture takes nothing from the stock index, and that mod gets **an index of its own** built from its `textures/` (one mod at a time, its own job ids and its own half of the kept store, its texel-matched files searched beside ours, and the game's font and the release's records kept out of it)
- **The Xbox 360 XBLA release: its textures, and drawing its models** — [xbla.md](CLAUDE-notes/xbla.md): the player's copy goes in `xbla/` the way a mod goes in `mods/` — the archive, the package, or a folder holding either, two deep, packages preferred — and a `.7z` is unpacked once into `cache/xbla/` (never into `xbla/` itself, which is the player's) by whichever of the conversion and the mesh loader wants it first (three seconds, 250MB, `xblaImportGetStfsPath()` blocks and `xblaImportIsAvailable()` does not); the STFS volume descriptor is one byte from a 257-block file table of garbage; LZX is a 17 bit window and a chunk is not a stream; Textures.raw is two 52-byte tables and the second is a D3DTexture, which is where the format and the tiling come from; pitch aligns to 32 texels linear and 128 block; a picture 16 or less on a side shares its tile with its own mip chain and **level 0 is then 16 texels in**, not at the tile's origin (the packed mip tail - 400 records, which decoded as their own mips with the picture missing and, since the explosion's colour ramp is 14x14 and a ramp multiplies, took every explosion off the screen while Enable Textures was on: 2026-09-11 to 2026-09-12) - and it is 16 *texels*, so a block format steps 4 of its elements, while a surface with only one level is not packed at all and starts at the origin (the fetch constant's packed mips bit; 2026-09-16, measured on GoldenEye's N64 pictures, which is what made every one of them black); record index *is* the texture number for the first NUM_TEXTURES and half of them still do not match stock because 4J redrew them; PackedSegFile slot *i* is file id *i+1* and its plain files are stored already inflated; the geometry is 596 files in 4J's own format, named by a mesh id in `modelnode`'s padding whose low 12 bits are a file id and so **one more than the slot** (reading it as the slot gives every model the mesh next door, which looks nearly right), drawn by `port/src/xblamesh.c` as a Perfect Dark display list — 1:1 in the model's own coordinates, 25 vertices to a batch, a list per group so the node carrying part p draws group p (and a piece the game toggles off stays off), all of them under the first part's matrix, and the node's own `G_MTX` copied or it lands nowhere; two menu checkboxes on the *Xbox 360 (XBLA)* page, meshes and textures separately, and a texture pack goes on underneath both and never has to come off (a mesh's records are past the numbered ones, so the two cannot collide); **Enable Textures is the whole of the release's art since 2026-09-11** - a numbered texture is served its record out of the package through the pack's own branch of `gfx_pc.cpp`, pixel-identical to the converted pack and behind any pack the player selected, so nothing has to be converted to see it (and the page's "Enlarged Textures Only" is gone, the button reading "Make Into Texture Pack"); both switches are live - the textures one because it is read where a picture is handed to the renderer rather than where a list is built, the meshes one because every model is matched as it loads whether or not it is on (a list of loaded modeldefs to go back over instead **crashes**: one can be freed inside a stage and its memory reused), which costs nothing measurable and never unpacks an archive (the empty "no package ready" answer is remembered, or every model load re-decides it - 57 opens of the player's archive over one level load - while the unpacking call still ignores it - and the level the switch was flipped in keeps its stock models, which is the one case on the page where a checkbox does nothing visible and the one line under it that is ever shown says so); the release's own **48 frame explosion** goes in the ROM's place one picture at a time (`port/src/xblaexpl.c`, `Mod.XblaExplosions`, "Enable Explosions"): 4J never upscaled the ROM's explosion records because their renderer drew its own animation at 0e1f-0e4e, the ROM's colour ramp **stays** on tile 1 (its alpha averages two fifths and replacing it with white made the fireball half again as strong as the game's), frame 0 stays the ROM's blank one, and two explosion runs a second apart in wall time are not comparable - judge it frame-exactly under `--fixed-step` and gdb or not at all; the release's **interface art** is catalogued in xbla.md ("The interface art") and its **logo** is the XBLA page's banner (`port/src/xblaui.c`, through `menuimage.c`, which can now name a loader instead of a PNG in the binary) - where the row order caught us out again, since the font atlases are stored top row first and the logo bottom row first; **the release's font is served the same way since 2026-09-11** (`port/src/xblafont.c`, `Mod.XblaFont`, "Enable Font"): seven atlases in records 0db7-0dbe with their cells in `DataFiles/*.abc`, one per font file and paired with the record by the size of the picture (two are 1024x670 and only the subject tells those apart), fitted on *ink* rather than by 4J's metrics - so the ROM's widths, baselines and kerning still lay the text out and nothing reflows - but on the ink of the **font** and not of the glyph, since a 16 texel bitmap's own box carries a texel of its rasteriser's rounding (the md 'A' has a row of spill under its baseline and the 'H' has none, which drew one a texel taller than the other: "some letters sit higher"), so the ink is measured to a fraction of a texel and one scale and offset per font is fitted through all 94 characters at once, the characters the ROM drew somewhere of its own - its '_' is an overbar - shrunk into their tile rather than squashed to fill its box (which drew the colon's dots as flat bars: "':' is cut off a bit"), and the xs font's lowercase drawn as the capitals it is written in; the tile's drawn band is rows 1 to height+1, so ink placed in row 0 is never sampled; and the outline pass's border is built here because the shader measures its half texel in texels of what was uploaded; a model drops whatever was registered against its own address as it loads, because a modeldef is freed and reused inside a stage and the draw path's definition test cannot tell that from a live one, which leaves `lvReset()`'s drop as the backstop for models never loaded again; textured by `port/src/xblatex.c` from the release's own records 3741-5746, which no pack can ship *by number* (an `xbla` folder in one replaces them by record, through texpack's queue and store, and the release's art is drawn until that decodes), through a 32x32 stand-in tile whose *address* names the record (the trick `texpackLoadReplacement` plays) and whose size is what a Vtx's 10.5 s and t can hold; the picture's rows are **not** flipped (a pack proves the decode order is the game's own) but the mesh's **`v` is**, D3D counting rows from the end the game's `t` counts away from — and a mirrored atlas does not look upside down, it looks like art that is simply wrong (sleeves across a chest, a waistband round the hips); skinned by `Mod.XblaMeshPose`, which is on: palette entry *i* is matrix *i* of the model, the palette holds each bone's **inverse** bind with a translation in its last column (not a bone position), and the header's float at +0x18 is the mesh's scale times 100 — a tenth for nearly every skinned mesh; heads are `Chead*Z` models grafted onto the body at its `HEADSPOT`, so they reach the hook with the body's model and the head's modeldef and are let through by walking the parents to the root through a headspot, and Joanna's three restructured heads pair by vertex count instead of node for node; a head's **stock hair** is a toggled node of its own that the release leaves at id 0 while marking a toggled piece it keeps with `0xFFFF` (54 of those, every one a gun's or a console's, against 232 zeros of which 166 are a head's), so a zero under a head's hat toggle draws nothing - decided by the head skeleton, not by the graft, since the Character page zooms on a head loaded as a model of its own; **a plain zero is the mesh's geometry, not the node's** — the id is written on one node of a model and the mesh named there is the whole model (`CcarringtonZ` is one named list of thirty, and its mesh is 4792 vertices over a fifteen-bone palette), so reading a zero as "keeps what it has" drew the N64 body inside every character and the stock sofa inside the Institute's, which is what "both models loaded together, intermingled" was; a zero now draws nothing (946 lists) unless it is a far LOD alternative (864) or a toggled piece (125: muzzle flashes, the six heads' glasses), and nothing is suppressed unless the mesh itself is named on a list the game draws beside them (`CheadgreyZ` names its only mesh on a toggled alternative), which is also why the node table is 16384 entries now; **a mod's model is left alone** — the release's package is keyed on the game's own file ids and a mod keeps the id while replacing the contents, so GE-X's file 447 was paired by size against `CbiotechZ`'s mesh, one list of twenty-five took the whole of it and the other twenty-four kept drawing through it, which is what "both models on screen at once and the hair floating above the head" is (`romdataFileIsStock()`, and since 2026-09-14 a stock file whose texture configs name a texture the mod supplies is left alone too - `modTextureExists()`, asked by number because the match runs before the configs load: GE-X kept the game's dataDyne props and repainted them, and they drew as the release's; plus the pairing by size having to account for every list it did not take — only a far LOD alternative may be left over); `--xbla-mesh-verbose` answers "are two models on top of each other" directly now, one `OVERLAP` line per (model, slot, reason); a matrix handed over as floats (`G_MTX_FLOATS`) also skips the **stage scale** `mtxF2L()` folds into the first three columns of everything else (`scale_bg2gfx`, a half on Villa, Crash Site and Air Base), which put every posed mesh at twice its true depth on those three - the right picture, because the scale is about the eye, and behind the floor it stands on in the z buffer, which is what "transparency issues with every xbla texture" was (`mtxApplyGfxScale()`); the pose arena is **a list of chunks** rather than one block a side, because a block can only be grown between frames and the frame that first wanted more drew a head a body's height above its body (one chunk a side for eight simulants, six for eighty); and four faults that were in here — `Mod.XblaMeshes=1` in pd.ini with the release still in its `.7z` drew nothing for ever because a model load never unpacks and the checkbox that would have was already ticked, the node registry never reused a tombstone and filled up over a match, the pose arena stopped growing at the frame that wanted more than the cap, and three bounds were a word short on a slot that a mesh id names but is not a mesh; and **F6 is the whole release** since 2026-09-12 (`port/src/xblaswitch.c`, xbla.md "The whole release from one key") - meshes, textures, rooms, font and explosion move together, two states and no memory of a mixed one, the page being where a mixed one is made; the pd.ini key is still `Mod.XblaMeshKey` so nobody's own binding is lost; a record is an **atlas**, so whether a rigid mesh's cutout triangle is really a pane (the Villa's glass table tops, the shadow blob under every chair) is asked under the triangle and not of the record (`xblaTexRecordAlphaMap()`, xbla.md "The pane in an atlas") - never of a skinned mesh, where the same test finds the hair and the sunglasses; a chr's **bruises** reach the mesh by mirroring the stock colour tables `chrBruise()` copies, through a per-model map and segment 5 (xbla.md "Bruises") - a map made in one pose since 2026-09-15 (body skinned into the stock rest, grafted head translated onto the stock head), because matching the release's bind pose against the stock rest read 60 of a body's 1371 stock vertices and the blood never showed; a **shot** at a chr whose mesh is drawn is traced against the mesh's posed triangles in `func0f06bea0()`, with the hit part taken from the hit triangle's bone rather than from the walk order, and only in single-player (`cheap` shots and shields stay boxes - xbla.md "Shots hit the release's triangles"); a rigid mesh can be authored with its first part's rest offset in it, which the part matrix already carries (the ejected shell orbited three units off its middle), and a destroyed prop is `objDeform()`'s jittered copy, which the door trim test used to read as a trim and flatten away - both since 2026-09-14 (xbla.md "Shells and destroyed props"); a skinned prop whose lists load no `G_MTX` was never posed and drew its bind pose, which for Chicago's chain-link gate (`PwirefenceZ`, z 35069) was rounded flat into the wall - posed under the node's position matrix when its bind box leaves the s16, 2026-09-15 (xbla.md "A skinned prop whose lists load no matrix"); and an "Unknown GBI opcode" in a room list on the Villa that seems to follow unrelated changes is **pre-existing and not understood** - it reproduces on HEAD byte for byte, and three explanations (segment 5, `bgLoadRoom()`'s list overlap, the mesh switch freeing rooms) were each disproved (xbla.md "Segment 5 is the model's"); build HEAD with `git stash` and run the same drive before blaming a change; one way to get that crash **is** found (2026-09-14, three v3.6.0 crash reports): F6 reloads every room from the other copy while dyntex keeps the first copy's vertex offsets for the level, so `dyntexUpdateOcean()` wrote into the new display lists - `dyntexForgetRooms()` in `xblaStageSwitched()`, and ASan hides it; and **the boot logos are the release's** since 2026-09-13 (orange Rare, Microsoft Game Studios, 4J's marble cube - xbla.md "The boot logos are the release's own intro"): a mesh named under a toggle filed no covered lists until then, the Rare mesh is ten times its plaque, the cube's mesh sits on morph targets the N64 never shows, and F6 is ignored while `titleIsBootSequence()`; the two 4J cubes draw **the release's reflections** (xbla.md "The release's reflections"): a material's byte 24 picks an environment cube map (records 0e93 on) and byte 16 is the percentage it blends towards it, found by logging texture bindings in Xenia, drawn as a sphere-map atlas in an additive pass (`G_ADDITIVE_EXT`, new in the renderer); and since the same day **every mesh** reflects behind `Mod.XblaReflections` (xbla.md "The reflections on every mesh": 231 meshes, 131 skinned; normals posed with the pose, the view taken from the node's own float matrix so the atlas serves any camera, the room's light as 255 minus the fog blend's alpha, no reflection without a depth buffer, and the title's cubes still pixel-identical); the **Reflection Cutoff** on the XBLA page (Dab's Display page until later the same day; still `g_ModOptions` and the preset) fades them out past `Mod.XblaReflectDistance` (15 m) and has to fade the dimmed base colours with them or they pop at the cutoff, and `xblamesh.c` is built at `-O2` beside port/fast3d (xbla.md "The cutoff and the faster loop"); and the lookup is **view space, confirmed** from Xenia's draw log of a turning Falcon 2 (xbla.md "The release's reflections are in view space": the gun's palette and projection do not change with heading, a virtual uinput pad drives Xenia into gameplay); and since the same day it is **per pixel** (`G_ENVMAP_EXT`: the vertex colour carries the normal, the renderer adds the view-space normal and position to the vertex, the fragment shader reflects and samples the atlas at level 0, the cell rides in s,t as whole-cell codes; xbla.md "Per pixel"); and a **Reflection Style** beside it (`Mod.XblaReflectStyle`: Xbox 360, or K7 Sheen ("N64 Sheen" until renamed), the default from 2026-09-13 until Level Metal took over as the default on 2026-09-14 - the stock K7 Avenger's own texgen spans, ROM texture `0x3eb` at `G_TEXTURE 0x0800` under `G_LIGHTING | G_TEXTURE_GEN` and `lightsSetDefault()`, drawn from a copy of the reflection list at 2.5x the release's amount and **added over undimmed colours**, since blending towards it drew darker than both games at every share; xbla.md "The N64 sheen"; except on the eight classic guns, where 4J put one reflecting material over the whole gun, so the added sheen turned the black PP9i to chrome - their share is weighted by the paint under each vertex, `xblaMeshInkFile()`, xbla.md "The classic guns"); and the title's marble logo can be drawn in the levels' own materials (`Mod.XblaLogoMaterial`, 2026-09-14, default on: faces in the Carrington statue's blue `0x0042`, bevels in the grey metal `0x006d` brightened with a glint pass, and the boot Rare logo under the guns' Level Metal with a depth buffer of its own, all live texgen; the title has no player, so the pass writes its own eye-space LookAt - `lightsSetDefault()` would dereference `g_Vars.currentplayer`; xbla.md "Logo Material")
- **Level Reflections, and the all-surface Level Sheen that was removed** — [level-sheen.md](CLAUDE-notes/level-sheen.md): the all-surface K7 sheen on rooms and props (`Mod.LevelSheen`, 2026-09-14) was judged "too shimmery, not realistic" and **removed the same day** at the user's request - do not bring a sheen back on every surface; what is left in `port/src/roomsheen.c` is the XBLA meshes' K7 movement scroll (`roomSheenTexgenShift()`, which the user likes on the weapons) and Level Reflections; the N64 texgen only sees heading, so a K7 pass sets `G_TEXGEN_EYE_EXT` to bend the lookup by the eye ray; its multiply blend (`G_MULADD_EXT`) went with it, while `SHADER_OPT_FOG_FADE` stays for `G_ADDITIVE_EXT` under fog; the levels already mark their own reflective surfaces (room lists turn on `G_LIGHTING | G_TEXTURE_GEN` over an environment map - Defection's metal, most windows), and **Level Reflections** (`Mod.LevelReflectFollow`) sets the eye flag round both room passes, which moves only those, and since the eye ray alone left them pinned to the screen while strafing, walking now turns their LookAt as turning the camera does (`G_TEXGEN_TURN_EXT`) - and the release's rooms keep every texgen mark the ROM's have, so it covers XBLA on too; **props mark their own too** (Defection's windows and lift are glass and lift objects whose model lists turn texgen on, ~90 prop models in all), so `objRender()` wraps each prop the same way (and `shardsRenderGlass()` a broken window's shards), and an XBLA mesh's K7 sheen inside it clears the turn flag and puts it back after (`roomSheenStockResume()`); `--boot-stage` takes the stage id (Defection 0x30, not its table row 0x1c); the Carrington Institute sits at frame 302 behind a dialog under `--boot-stage`, which is not a hang
- **Model packs and the asset dump** — [model-packs.md](CLAUDE-notes/model-packs.md): `model-packs/<pack>/n64/` and `xbla/` hold OBJ replacements named by the ROM's file names, read through `objmesh.c` and drawn by the XBLA mesh loader (an OBJ is written back out in 4J's own layout and goes through the same builder); the one **Dump All Assets To Disk** row (`assetdump.c`, `--dump-assets`) writes `texture-dumps/<romid>/` (+`xbla/`) and `model-dumps/n64/` and `xbla/`; an N64 model's list pointers are *not* promoted with the rest and stay segment 5 until the texture rewrite; a group of an N64 model is a list node in the node's own space and the dump adds the rest offset the loader takes off; skinning does not survive OBJ and is transferred by nearest vertex; and everything about a pack is **live** since 2026-09-11 - both halves of a node (the release's mesh and the pack's file) are filed as every model loads, whichever draws is decided at the draw and settled by **Mod.ModelPackPrefer** where a model has both, F9 re-reads an edited OBJ, and a material named for one of the ROM's textures binds the *ROM's* picture plus its number so a texture pack repaints it (the dump's `map_Kd` points out of the pack and is a reference, not the author's picture - reading it as one is what stopped that)
- **GoldenEye 007 XBLA (Rare's "Project Bean" build) characters on GoldenEye X** — [ge-bean.md](CLAUDE-notes/ge-bean.md): `port/src/gebean.c`, `Mod.XblaGoldenEye` ("Enable GoldenEye Characters", default off); the archive or its folder goes in `xbla/` beside the Perfect Dark release, and only `files/new/char/` and `head/` are streamed into `cache/xbla/goldeneye/` (`archiveExtractMatching()`: the 7z is one 740MB solid block that `SzArEx_Extract()` would hold whole), while `xblaimport.c`'s scan now skips an archive holding them - the first `.7z` in `xbla/` used to be taken for Perfect Dark's whatever it was; CAFF 07.08.06.0036, nothing of the Perfect Dark release's formats applies; GoldenEye X files pair with Bean assets through the generated `gebeantable.h` (name plus list node and vertex counts, mods only - regenerate with `.xbla-work/ge-bean/gen_beantable.py`); the UV scale is 1/16384 or 1/32768 with no marker (measured per file) and a vertex buffer is shared by draws with different palettes; the mesh is 4J's layout skinned to the model's own matrices with each joint's rest as the inverse bind, fitted onto GoldenEye's star rest pose, a group per list node, heads rigid on matrix 0; bruises and the triangle hit test do not reach it yet; a `bean` model pack on disk wins over it; and **F6 switches its look** (2026-09-15): with the release's meshes off a Combat Simulator pool character draws Bean's N64-look `files/original/` and a GoldenEye X one its own N64 model - the originals' UV scale is a per-file power of two (2048-16384) measured from the median texture, they draw with a second record (0x30, whose fourth word is a *piece* number and not a flag - a head's sunglasses are pieces to leave out, a gun's hand and working parts are the model), sunglasses sit behind 0x17 kind 2, and four heads carry a zero pose that the HD file's neck replaces; **the guns followed F6 too** (2026-09-16, ge-bean.md "The guns follow F6 too", **removed the same day**: a gun's N64 look is now GoldenEye X's borrowed model, and with no GoldenEye X its rows are hidden in that look): a GoldenEye gun stands on a Perfect Dark weapon, so with the meshes off it drew GoldenEye's own N64 gun out of `files/original/gun/` - with the glove GoldenEye drew round the pistols and the knives, Perfect Dark's hands off those seven, the painted muzzle flash found by its bones and left out, the placement measured on the gun alone so both looks stand in the same place, a muzzle offset per look, and the material's picture read from the four-input shape GoldenEye's own models use (which also repaints the N64 pickups and one N64 head, and moves nothing else in either look); and **its weapon sets** (ge-bean.md "GoldenEye X's weapon sets, borrowed the same way": slots are weapon numbers mapped to the borrowed guns, `mpApplyWeaponSet()` searches past Disabled, the set list grows with `WEAPONSET_RANDOM*` after it) and **its arenas complete** (ge-bean.md "GoldenEye X's arenas, borrowed complete": the Stage Loader's GE-X maps take its stage rows, skies and model states - props - swapped per stage in `setupLoadFiles()`, weapons' states kept) and **its music** (ge-bean.md "GoldenEye X's music, borrowed the same way": sequences after the game's 119 each on its own instrument bank, swapped only when it changes, and its 44 tracks in the Combat Simulator's list, `MP_MAX_TRACKS` 96) and **GoldenEye X's characters are borrowed too** (ge-bean.md "GoldenEye X's characters, borrowed the same way": its own Combat Simulator bodies and heads replace the release's pool, names out of its language file, `NUM_HEADSANDBODIES` 512, and the HD look finds its meshes by name as with GoldenEye X loaded); and **GoldenEye X's guns are borrowed whole** when it is installed (2026-09-16, ge-bean.md "GoldenEye X's guns, borrowed whole", `port/src/modborrow.c`): its definitions out of its data segment, its models pinned to a guns-only mount with their textures by mod (`tex.srcmod`), its changed animations and its GoldenEye sound bank appended after the game's own - and an external animation's header and frames must go through the slot buffers or the bit reader never finishes; and **every one of GoldenEye's N64 pictures decoded to solid black** until 2026-09-16, which is why the N64 guns were black in third person - the packed mip tail's step is 16 *texels* and was being walked as 16 elements, four tiles past a DXT picture, and a surface with one level is not packed at all (ge-bean.md, "Every N64 gun black in third person"; it fixes 26 of the release's own records too)
- **GoldenEye XBLA's HD levels on GoldenEye X's rooms** — ge-bean.md, "GoldenEye XBLA's levels on GoldenEye X's rooms": `port/src/gebeanstage.c` writes Bean's material-sorted level mesh into GE-X's rooms in the ROM's room format and serves them through xblastage.c (live on F6); paired offline by room count + room-position hash (`.xbla-work/ge-bean/gen_stagetable.py`), GE-X's coordinates being GoldenEye's divided by the level scale; triangles go to the room whose *triangle* is nearest (a vertex names the room next door), a room is Bean's only if Bean covers 97% of its surface with no big GE-X triangle missing; level UVs are 1/1024, 32-bit index buffers exist (descriptor word 6), vertex arrays must start 8-aligned with an even count, t is flipped
- **GE Plus's arenas converted from the player's GoldenEye ROM at startup** — ge-bean.md, "The converter runs in the game, from the player's ROM": `port/src/geconvert.c` is `tools/geconvert/geconvert.py` byte for byte (numpy's pairwise mean, half-even rounding, no -0.0 from `float(round())`), everything read from the ROM's data segment; a US ROM in data/ is converted once into `mods/GoldenEye Arenas/` before the mods mount, with a notice drawn in fill rectangles, and the first conversion turns its maps on in `Mod.MapMods` and saves pd.ini at once; and **GE Plus opens on GoldenEye's own folder screens** (ge-bean.md, "GE Plus opens on GoldenEye's own folder screens", `port/src/gexfront.c`): folder, fonts, strings, pictures and the folders theme all converted out of the ROM into `menu/`, GoldenEye's text renderer rather than Perfect Dark's, `mtx00015f04()` is GoldenEye's scale (not `...f88`), the keyboard's accept is `BUTTON_UI_ACCEPT` not A, and gdb needs `'gexfront.c'::g_Front`; and since 2026-09-17 **SELECT MISSION opens GoldenEye's own solo chain** (ge-bean.md, "Solo missions on the folder screens") - mission select, difficulty, 007 options and the five-page briefing - which since the same day plays **GoldenEye's own twenty missions, converted from the player's ROM** (ge-bean.md, "GoldenEye's own solo missions, from the ROM", `tools/geconvert/gesolo.py` and the same code in geconvert.c): GoldenEye's setup file is Perfect Dark's ancestor and its propdef type numbers are Perfect Dark's own, so a mission converts record for record, one Perfect Dark record for each of GoldenEye's so the indices still line up; a `missions` block registers each as a stage that is **not** an arena; the AI bytecode is mapped 253 commands to 482 by `.xbla-work/ge-arena/genaitable.py` (the two tables aligned in order, and GE-X as an oracle) - **the aligner's tail past GoldenEye's 0xd8 is junk and anything uncertain there is dropped, because a wrong row crashes and a dropped one does not**; a guard's body is GoldenEye's own character number that `gexPlusMissionSetup()` maps at load to Bean's pool, GE-X's borrow or Perfect Dark's own; hats are left out and a weapon's model is left alone; **the missions' own text is the level's own bank served as a bank** (`LANGBANK_GEMISSION`, `gexPlusMissionLangLoad()`) - a GoldenEye text id is `bank * 0x400 + slot` and the bank is always the mission's own, so the conversion writes the slot against that one for an objective's text and for every `TextPrintTop`, and the file has to go through `preprocessLangFile()` or `langGet()` reads its 32-bit offsets at the width of a pointer; and **a pad exactly level with its floor has no ground under it** (Perfect Dark's ground search takes the highest floor *strictly* below), which is what dropped the player out of Egyptian and Control - `write_pads()` lifts those 63 pads a centimetre, and all twenty missions run now. With no conversion it falls back to **GoldenEye X's missions**, which are live only when GE-X is the mod the game is *loaded* with and not one it is borrowing from (`modBorrowIsGoldenEyeLoaded()`); GoldenEye's 007 difficulty is Perfect Dark's PD Mode and its four sliders are the same multipliers except the reaction speed, which the port does not act on; the briefing text is the ROM's, two more files a mission in `menu/` (the briefing file and the level's text bank, a text id being `bank * 0x400 + slot`), and the folder model's part 21 is the mission grid, so a mission's photo is `22 + mission`; and since 2026-09-18 **entering GE Plus plays GoldenEye's own intro** (ge-bean.md, "GoldenEye's own intro on the way into GE Plus", `port/src/geintro.c`): the gun barrel, the GoldenEye logo and the cast reel, then the folder screens - which needed the conversion to learn **GoldenEye's characters** (`tools/geconvert/gechr.py`: `c_item_entries` at data 0x1d080, all 80 written as `files/Cgx%03dZ`, and GoldenEye's `ModelJoint` skeleton is Perfect Dark's `g_SkelChrJoints` joint for joint so they load with `SKEL_CHR`) and **GoldenEye's animations** (`tools/geconvert/geanim.py`: its own bits with the root channels moved into the front of every frame, which comes to Perfect Dark's own 162-byte character header) - **an animation's root descriptors and stream are where its record's third and fifth words point**, offsets into `animation_data` - the blocks sit *between* the records, so reading them at `record+0x14` gave every animation the *next* one's, which is why the gun barrel's Bond stopped walking and dropped to the floor when he turned to fire (ge-bean.md, "The gun barrel: two faults in one sequence"); and **a character's animated parts are numbered from 1 in GoldenEye and from 0 in Perfect Dark**, which is what drew Bond lying on his side with his limbs posed correctly, and **the animation's root motion has to be carried into the model's position** by `modelUpdateInfo()`, which is Perfect Dark's own `subcalcpos()` - once a frame after the two ticks, not once per tick, or Bond walks in at twice GoldenEye's speed; and since 2026-09-18 (ge-bean.md, "The gun barrel: two faults in one sequence") **GoldenEye's hips must not be turned twice** - its header node applies no joint rotation while Perfect Dark's chrinfo *is* the hip node, so the group GoldenEye numbers part 1 is converted to a `POSITIONHELD` node and `bond_eye_fire`'s ninety degree turn stops coming out as a hundred and eighty; and **the cast reel** (ge-bean.md, "The cast reel: four faults, all of them GoldenEye's own numbers") is the **one** front-end screen GoldenEye draws into a z buffer - without it a character paints its far side over its near one - and its camera aims at the character's **root matrix** (its middle, 62) and not at the chr's position (where it stands, 10.9), with the ±100 height swinging the camera alone; its caption is on GoldenEye's own 315/108/152/174, which are already in 440x330; a head's sunglasses toggle and the gun's first two switches come off; and every model in the intro is lit by `gelogolight` and a reflected LookAt or it takes whatever the menu before it left behind. and **a texture a model file carries itself is named in its lists by its own address**, which the conversion was not moving with the data - the port's preprocessing rewrites such a `G_SETTIMG` only through an exact match against the texture rows and answers zero otherwise, so the GoldenEye logo's red ellipse read the head of the file (alpha zero) and its letters sampled 24 texels into their own picture (geconvert version 10; `$E/mods` is searched before `./mods`, so convert into `build/mods` before judging a converter change); and **the gun barrel** (ge-bean.md, "The gun barrel: Bond's tuxedo, and the 4:3 box") drew Bond's tuxedo flat black because the sight's own picture leaves `G_TP_NONE` set and nothing turned texture perspective back on for the model - GoldenEye's Rare logo, which this port does not play, is what leaves it `G_TP_PERSP` with `G_LIGHTING` and `gunbarrelLights` - and its 440x330 box now **covers** the window rather than fitting inside it, so a wide one loses the top and bottom of GoldenEye's picture instead of showing its edges sliding across black beside it; and **an animation's loop bit belongs on the animation** - GoldenEye's record carries it (`unk07 & 1`) and it is Perfect Dark's `ANIMFLAG_LOOP`, which is what wraps a frame past the end instead of holding the last one, and neither game's intro ever asks for a loop at the site that plays it - so `entry.flags = 0` in `introLoadAnims()` froze the cast reel's 26-frame `running_one_handed` in mid-stride for two of its three seconds (ge-bean.md, "The cast reel's runner stopped in mid-stride"); and **a row of `menu/intro.bin` is matched by its name**, which was a 20-byte field the converter truncated to 19 - so the ten animations with longer names (and `fire_standing_draw_fast`/`_slow`, which share their first 19 characters) matched nothing and **ten of the reel's twenty-two characters stood still**, the loader falling back to `idle` and saying nothing; the field is 32 bytes and the magic `GEI2` since converter version 11, and the loader names every animation the conversion has not got (ge-bean.md, "Ten of the intro's animations never loaded at all"); and **GoldenEye's blood runs on both of its screens** since 2026-09-19 (ge-bean.md, "The blood: GoldenEye's death screen, and why the barrel's wash never showed", `port/src/geblood.c`): one 42-frame animation out of the conversion serves the gun barrel and the death screen, and Perfect Dark kept the whole of GoldenEye's death but the animation (`redbloodfinished`, the red wash, the fade and the death camera are still there in `playerRenderHud()`), so a death on a converted mission or on GE Plus's arenas is two hooks and no new state - and **the renderer keeps a texture by the address it was uploaded from**, which is why the barrel's wash only ever drew its first, nearly empty frame: a buffer redrawn in place needs `videoFreeCachedTexture()` after every step; and **the gun barrel plays at the console's speed** since 2026-09-20 (ge-bean.md, "The gun barrel ran at twice the console's speed"): GoldenEye steps it once a *frame*, and a capture of the console shows the dots at sixty and everything from the sight on at a steady thirty - which its PAL constants confirm and which `M_INTRO`, started with the dots, is written to - so `introTickBarrel()` is a sixtieth of a second off `diffframe60freal` and half a GoldenEye frame from mode 3 on; ares draws it all at sixty and cannot answer a frame rate question; and Bond's camera follows the barrel's cover scaling on a wide window, where he had stood small and left of the lens's middle; and **GoldenEye's own sound effects play out of the ROM** since the same day (ge-bean.md, "GoldenEye's own sound effects, and the gun barrel's shot", `port/src/gesfx.c`, converter 39): its sfx bank converts as `menu/sfxctl`/`sfxtbl`, `geSfxPlay()` appends a sound the way modborrow.c does, the id is GoldenEye's `SFX_ID`, which is the bank's sound **id - 1** (GoldenEye reads its instrument through a struct that puts `soundArray` four bytes early; read from 0 until 2026-09-21, every sound played was the enum's next one), a key map's chain to a next sound cannot be rewritten to an appended id, so `geSfxPlay()` starts every link itself (each waits its *own* `velocityMax`), the volume is GoldenEye's own balance (effects at full over `g_musicDefaultTrackVolume[]`, scaled to the 0x4ccc its music plays at here: 0x5555 in the intro, 0x5fff elsewhere) - **a YouTube capture's limiter halves a transient, so measure loudness against the oracle's `PORT_AUDIO_WAV`** - and the folder screens and the watch play GoldenEye's own few sounds and are silent where it is (a cursor or value moving, a press on nothing), and the solo watch has GoldenEye's **interference** (`watchTickStatic()`: the face's green is the fill's alpha, and `G_RM_AA_PCL_SURF` is the snow because **a render mode's `G_AC_DITHER` bits do reach the hardware** - the microcode ors the whole word in and they stay set - with a scanline whose alpha wraps in a byte), its pages **fit the face at any window** (a text frame with no aspect mode is not square - a third too wide on 16:9; `frontWrap()` never measured a paragraph's last word; values at GoldenEye's 0xb4; the layout at 0.88), and **the gun in hand is held up on the face** (`watchDrawGun()`: the conversion's `Igx%03dZ` first person models, all 25 of which draw, placed by `menu/geitems.bin` - `gitem_structs` whole, converter 40, **C only: the Python converter has no item conversion** - under GoldenEye's 45 degree camera squeezed onto the text's frame, parts 8-13 hidden and **14-15 shown**, which are the throwing knife), and **GoldenEye's controller on the control page** (`watchDrawController()`: `GjoypadZ` is hand item 0x55 and converts like a gun, `Igx085Z`, converter 41; it turns only by the player's stick while they hold the page's second row and eases back after a hundred frames; the second pass of loose buttons beside their names is not done); and **Perfect Dark's own guns show on it too** (`watchDrawPdGun()`: the inventory menu's and firing range's `gunconfig[]` table, now at file scope in the port behind `menuGetWeaponModelConfig()` - its scale is already in view units - with `weaponGetFileNum()` and the weapon's `partvisibility`)); and since 2026-09-19 the folder screens' **background reaches the edges of a wide window** (ge-bean.md, "The folder screens' background on a wide window") - the folder and its text already keep GoldenEye's shape in the middle of any window, and what was black beside them is the backdrop, which is a **picture frame of eight vertices** and not a plane, so only its outer four move (`frontWidenBackdrop()`) and they move to the half width the *camera* spans rather than by the window's aspect, GoldenEye's own right edge being inside its 4:3 screen already; and the same day **everything 2D on those screens was a twelfth too far from the middle** (ge-bean.md, "PREVIOUS stood beside its tab, not on it") - `G_ASPECT_CENTER_EXT` holds this frame at `SCREEN_ASPECT` (320/220), *not* 4:3, so GoldenEye's 440x330 goes into it by its **height** and centred (`frontX()`, `frontScaleX()` == `frontScaleY()`), and the folder, drawn through `videoGetAspect()` with no aspect mode, was right all along; and since 2026-09-19 **a converted wall stops under the floor above it** (ge-bean.md, "Walls in the air over a staircase", `wall_above()`/`wallAbove()`, converter version 16) - the conversion raises a quad round every unlinked stan edge and used to raise it 400 units, while **GoldenEye's own wall is the edge and nothing more** (`sub_GAME_7F0B1DDC()` walks out from the tile the player stands on through the links alone and never looks at y), so wherever two walkable surfaces sit over one another the lower one's walls stood in the air across the upper one - which is every level with stairs, GoldenEye stacking its flights - and the traps in fixing it were that a riser's own side is a wall that is a *point* in plan, that a wall blocks between its own lowest and highest vertex rather than along its plane (`cdCollectGeoForCylFromList()` reads the tile's ymin/ymax), and that the player stands up to their own radius from it, on ground that much lower; and the same day **a wall's foot lifts over the head of anyone standing under it** (ge-bean.md, "And a wall hanging into the space under a ledge"), capped at 50 over its own edge and not made at all where the lift would have to go further - **players crouch twice**, and `playerGetBbox()` still holds their box at manground+30..+80 however deep (`minsane`), a chr's deepest duck being `chr->height` 90 over manground+20 - so most of what looked like this class is a raised platform a player belongs against, and **counting whether a position is blocked says nothing**: ask `cdCollectGeoForCyl()` which geo answers (`build/gexrom/blockprobe.py`), since a place under a walkway has a real wall beside it too; and since 2026-09-19 **a converted wall blocks sight and shots as well as bodies** (ge-bean.md, "Guards alerted across the map, and shooting through walls") - Perfect Dark reads a tile's flags for the question being asked and nothing else, so a wall carrying `GEOFLAG_WALL` alone stopped a body and was not there at all to a guard's sight (`GEOFLAG_BLOCK_SIGHT`) or to a bullet (`GEOFLAG_BLOCK_SHOOT`), which is "they shoot you through walls from across the map"; two AI rows in the same report told every guard to go looking for the player on its first tick (`IFISeeSomeoneShot`/`Die` are `aiIfSawInjury`/`aiIfSawDeath`, not a line to the player and a route to it), Perfect Dark's `aiIfChrHealthGreaterThan`/`LessThan` are named the other way round from their own bodies, and **`.xbla-work/ge-arena/genaitable.py` is stale and must not be run** - the AI map is kept by hand in `tools/geconvert/geaitable.py` and `port/include/geaitable.h` together; and the same report's cinema half (ge-bean.md, "The cinema's camera was the player's prop") - GE Plus's Cinema moved the **player's prop** to each shot, so a guard's list, which asks about the prop and not about the picture, found Bond standing at the camera: `g_Vars.bondvisible` covers everything that asks whether a guard can *see* him but nothing that asks where he *is* (`aiIfTargetInRoom` alone is 264 commands over the twenty missions), so the camera is placed on its own now with `playerSetCamPropertiesWithRoom()` from `lvTickPlayer()` - after `playerTick()` has built its own from the eye - and the prop stays at the spawn, which is where GoldenEye leaves Bond; and since 2026-09-19 a converted mission runs **GoldenEye's own guard AI and wears its own characters** (ge-bean.md, "GoldenEye's own guards, wearing its own faces and running its own AI"): GoldenEye keeps eighteen global AI lists that every level shares (`g_GlobalAILists`, data `0x8003744c`) and names one by an id of **1024 or less**, which is exactly where Perfect Dark's `ailistFindById()` divides the stage's lists from **its own** - so a guard whose list was GoldenEye's standard guard has been running Perfect Dark's unalerted list since the missions first ran, and the sight code was never the difference (`chrCheckCanSeeTarget()` is GoldenEye's `chrCheckTargetInSight()` line for line, and Perfect Dark's `chrsCheckForNoise()` only records a time where GoldenEye's alerts); the eighteen convert with each mission at ids from **0x800** - they must stay under 0x1000, since Perfect Dark makes a *background chr* of every stage list from there up and ticks it from frame one, which had `m_RunToBond` crashing in `chrGoToRoomPos()` with no prop - the table is sorted and holds each id once (Perfect Dark binary-searches it; Facility carries 1063 twice), and **`--ai-trace [frames]`** prints a line per AI command per chr with the list as `ailistFindById()` resolved it, which is how to answer "what is this guard being told to do"; and a mission's guards are GoldenEye's own eighty characters out of the ROM (`menu/gechrs.bin` for each one's flags, scale and pov, which are `makeonebody()`'s two `modelSetScale`/`modelSetAnimTranslationScale` arguments), rows taken from `g_HeadsAndBodies` per mission and given back by `gexPlusMissionHeads()` on any stage that is not one - a body's row must be under 256 (`packedchr.bodynum` is a u8, and so is `aiSpawnChrAtPad`'s), a head's need not be, since GoldenEye's own setups leave all but two heads at -1 and its `bodyChooseHead()` takes four male heads for a level and one female exactly as Perfect Dark's does; and since 2026-09-19 **a mission's own cutscene camera goes where GoldenEye put it** (ge-bean.md, "A converted mission's own cutscene camera"): GoldenEye's `CameraSwitch` becomes Perfect Dark's `ai00df` and the record it names is Perfect Dark's `cameraposobj` field for field, so it was copied raw - which is nearly right, because **`setup.c` still does GoldenEye's own conversion on it after the load** (the position over 100, the angles over 65536), and what a raw copy misses is the level's offset, so every one of the thirty-one shots in the twenty missions stood at GoldenEye's origin; the yaw is **mirrored** between the two engines and not turned half round (GoldenEye's z is -cos·cos where `playerExecutePreparedWarp()` builds +cos·cos - a third convention again from the player's own basis, which is why the Cinema page wanted +180), the look-at-Bond flag (`CAMERAMODE_INTRO`, six of the thirty-one) has no case in Perfect Dark at all, and **the player has a chr and a prop at all times but only a model while something draws their body**, so the five missions that teleport Bond on the frame their cinema starts crashed `chrMoveToPos()` on a NULL model - which is what "a converted list parks at its CameraSwitch" really was; and the same day **GoldenEye's vehicles run** (ge-bean.md, "GoldenEye's vehicles, which nothing had ever ticked", `port/src/gexplusveh.c`): Perfect Dark still has both of GoldenEye's vehicle records - `truckobj` is its `VehichleRecord` and `heliobj` its `AircraftRecord` field for field - and **no tick for either**, nothing outside chrai.c so much as naming `g_Vars.truck` or `g_Vars.heli`, since the hovercar and the chopper took the job over; the record's **AI list id** sits at GoldenEye's 0x80 = Perfect Dark's 0x5c and had no `OBJ_TAILS` row, so `ailistFindById(0)` handed all seventeen vehicles Perfect Dark's own global list 0 (the guards' fault of `1afd53740` in the one place it was not looked for); all three vehicle AI commands are Perfect Dark's own line for line, so what was missing was GoldenEye's tick, and the rotor's angle is advanced in the **render** on one node's matrix (part 2, the numbering surviving the conversion) rather than at the fan's hook, which turns the whole object; **and its three animations fly it** (ge-bean.md, "And the three animations an aircraft flies by"): `animation_table_ptrs2[]` is one animated part and the four root-motion channels where a character's is fifteen parts, its ids **collide with the guards' table** so they take an id space of their own from `GEVEH_ANIM_FIRST` (256) and a vehicle's `PlayAnimation` becomes the port's own `aiGeVehicleAnim` (0x01e2), the aircraft model keeps its **chrinfo node** now that `modelUpdateChrNodeMtx()` tolerates a model with no animation (GoldenEye's own answer: the node's matrix is its parent's), and the flight is the animation's **root motion** through `modelSetRootPosition`/`modelUpdateInfo`/`modelGetRootPosition` with the height from the record's own pad - and the two silent traps are that **a prop's model carries no `anim` until something gives it one** (`modelSetAnimation2()` is a no-op without it) and that **an anim slot is only marked free, never cleared**, so a `playspeed` of zero left by its last owner holds frame 0 for ever; and since 2026-09-19 **GE Plus pauses on GoldenEye's watch** (ge-bean.md, "GoldenEye's watch as GE Plus's pause", `port/src/gewatch.c`): the gun goes down, the view tilts to -40, GoldenEye's own arm comes up playing its `bond_watch` and the view zooms to 5.9 degrees, with its five screens on the face (mission status and its abort, inventory, control, options, briefing) and its multiplayer overlay in the arenas - GoldenEye's state machine and its own strings, and the faults were that **a model instance belongs to the stage pool** (never free one after its stage, and `modelmgrAllocateSlots()` runs later in `setupLoadFiles()` than a stage hook, so the arm loads at the first pause), **`renderdata.unk00` is written by `modelSetMatrices()`** so a pose handed in is gone by the time the model is posed, **the watch's three hands are parts 0, 1 and 2** and not the first three `POSITIONHELD` nodes in the tree, the pose is reasserted on `matrices[0]` **after** the animation (rotX(+90), less the hour node's own offset), and **GoldenEye's in-game screens are laid out on its 320x240 frame** where the menus are its hi-res 440x330 (`gexFrontTextFrame()`, and since the watch pulled back it is over the **face** rather than the viewport, so the screens stay on the watch at any zoom); the arm is posed at the player's own wrist and the **whole** pose - not just the root, as GoldenEye moves it - is carried to the watch, since this window is wider than GoldenEye's 4:3 and has room for the cuff and the hand beside the face, and the open zoom follows the window's shape (5.9 degrees on 4:3, 11 by 16:9); and since the same day **the arm is the player's own character's** (ge-bean.md, "The player's own arm wears the watch"): `bond_watch` is a character animation and GoldenEye's floating arm is one of its own characters, so the animation poses the player's own body, every list outside its arm is put away for the draw - following **distance nodes' targets** to find them, and clearing the *instance's* `rwdata->dl.gdl` rather than the definition's pointers - and GoldenEye's own watch model (`GwatchidentifierZ`, out of `gitem_structs` at data 0x12b94, converter 35) goes on its `MODELPART_CHR_LEFTHAND`, where a position node's `pos` is the bone that built the matrix and not an offset to hang anything at - and **nothing done to the watch can turn it on the screen**: the move to the eye is `pose * inverse(face)`, so a roll on the watch turns the *body* under it, the dial's look is the pose against the face's axes (GoldenEye's rotX(+90) is for its hand bone, and the watch model is a quarter turn off it), and the fit on the wrist is measured off the floating arm, which wears the same watch on the same bone; the watch is **life size** (0.1 - anything more shrinks the whole body towards the eye, which is where the headless shoulders came from), its hands are **vertices of its one mesh** turned in place, the body wears a copy of its head that goes when the near plane would cut it, and **the conversion read a type 4 list record's one-byte render mode as a word until version 36** (3 became 0x0300: no render mode, and the second list never drawn) - which was GoldenEye's floating arm drawn as a pale sleeve and an icy hand round a white dial, its watch item, and the 21 third person guns and one hat missing their trigger guards, stocks and sights; *not* every character, as first guessed from the arm (count with `build/gexrom/gemodes.py` before saying how far a fault goes); fixed with 137 before/after pictures in `build/gexrom/cap/`, and with the arm right **the watch is GoldenEye's own arm again**, the player's body kept behind `g_WatchOwnBody` (ge-bean.md, "The render mode, fixed in the converter"; the intro is captured from `--boot-stage 0x26`, never from a mission)
- **A converted level's doors and pickups sound like GoldenEye's** — ge-bean.md, "A converted level's doors, its body armour, and the index that was one out": the conversion always carried GoldenEye's door sound type and Perfect Dark read it through its own table; `geSfxDoor()` answers propobj.c's four door moments from GoldenEye's switch (a sound is either let go to ring out or *held* - the looped slide the door's next sound stops), through an audio config of GoldenEye's own falloff; `geSfxPickup()` maps the seven `SFX_PICKUP_*`; **a sound that plays and is plausible proves nothing about which sound it is** - dump the bank and check a property the name predicts; `doorsnd.py`, and `runall_snd.sh` is the sweep with the sound on
- **GE Plus's Cinema, and staying inside GE Plus** — ge-bean.md, "Stuck in the Cinema, and GE Plus's own main menu": a cinema read the **N64's** buttons alone, and in the port's PC scheme `CK_A` is unbound, `CK_B` is `E`, Escape is no bind at all (it is read with `inputKeyJustPressed()`) and the **right mouse button is `CK_RTRIG` as well as `CK_CANCEL`** - so Escape did nothing and a right-click *skipped to the next shot*; it reads what the folder reads now, leaving asked before skipping. And a cinema came back to `STAGE_TITLE` with the folder drawn over it - **the title is not a backdrop**, it ticks on underneath, reads the same presses and after twenty seconds loads its **attract demo**, which resets the model pool the folder's own model lives in. Both a cinema and a mission go back the way a **match** does (`gexFrontGoBack()`: the Institute, `TITLEMODE_SKIP` and `var80087260`, which skips the Institute's arrival and is what `menuTick()` waits on to push the Perfect Menu). And **while inside GE Plus its main menu is the main menu** (`gexFrontIsInside()`, true until the player backs out of the mode select): a mission's endscreen - finished, failed or aborted, all three through `MENUROOT_ENDSCREEN` - comes back to the folder instead of dropping out to Perfect Dark's title. A `--boot-stage` run has no agent file, so its endscreen is the **"Error" dialog** that no press dismisses; drive the root or call the hook
- **A converted mission ends on GoldenEye's report and statistics pages** — ge-bean.md, "A mission ends on GoldenEye's report, not Perfect Dark's endscreen": `gexFrontMissionReport()` stands in `mainEndStage()` where `endscreenPrepare()` does, keeps how the mission went (the level is gone when the pages draw) and goes back to the folder on `SCREEN_REPORT`/`SCREEN_STATS`; Perfect Dark's endscreen had been **filing a converted mission's time under the same number in Perfect Dark's own save** (Dam completed Defection), so best times are the remake's own `$S/geplus-times.txt`; `--boot-stage`, the GE-X fallback and co-op keep Perfect Dark's endscreen; Dam is stage **0x15**; `build/gexrom/report.gdb`
- **A converted mission's opening and its ending** — ge-bean.md, "A mission's own opening, and the endings Bond was never in": a mission plays GoldenEye's still, fade and **swirl down to Bond** before the player has control (`gecinema.c`; Perfect Dark kept GoldenEye's frozen camera as `TICKMODE_WARP`, so the swirl is that mode with a camera of its own, the `INTROTYPE_SWIRL` records converted to floats in the file, converter 42), wearing **GoldenEye's own Bond by the setup's outfit** (`gexPlusMissionBond()`, asked in `playerTickChrBody()` and *not* `playerChooseBodyAndHead()`, which the first person hands and the watch's sleeve also ask); every **ending is Bond's own AI list** (`SetBondsAiList`) and never ran because `HideAllChrs` hid the player's chr, which GoldenEye does not have yet when it runs; **`TRYGiveMeItem` carried GoldenEye's raw prop and item numbers** (105 commands: every guard a list arms held Perfect Dark's model of that number - a lab door, an ammo crate), and `IFBondHasItemEquipped` its raw item, which is why Surface 2's spawners changed; **a tag that points at a four-byte "nothing" record** had `OBJHFLAG_TAGGED` written 0x40 bytes past it and crashed Streets; `--skip-mission-intro` for probes, `runall_intro.sh`/`runall_intro_t.sh` for the sweeps with the opening; build HEAD beside a change (`build/gexhead`) before believing an old sweep log
- **GE Plus's EXTRA page, the Cinema's Intro/Outro, and GoldenEye's monitor programmes** — ge-bean.md, "EXTRA: the Cinema on the mission grid, outros, and the monitor programmes" (converter 44): the mode select's third row is **EXTRA** (1. Cinema, 2. Monitor Programmes); the Cinema is the mission select's slide grid and then **Intro** (every opening shot, then the fade and the swirl) or **Outro** - found by the pair every mission ends on, `HideAllChrs` + the exit command, with a background chr started on it; `--cinema-opening`/`--cinema-ending` and `runall_cinema.sh`, where **rc 124 after an "over at frame" line is a pass**; **leaving either Bunker crashed the game and always had** - a GoldenEye hanging TV attaches to its mount through `OwnerOffset` (0xf4 of its record), the conversion carried no monitor tail, so it hung from *itself* and `objFree()` recursed for ever at the unload, which no sweep had ever done (`leave6f.gdb`); and **a converted level's screens ran Perfect Dark's programme 0**: `tvscreen` is GoldenEye's monitor command for command, so its 52 programmes (one block at data 0x80030b74, 1334 words) and 50 pictures convert as `menu/gemonitors.bin` and `port/src/gemonitor.c` answers propobj.c's three questions (which programme, where a jump goes - a word of the block, since a list is 32 bit and a pointer 64 - and which picture); **Monitor Programmes** shows them twelve to a page on GoldenEye's own TV sets (`PROP_TV1`, `frontDrawTvs()`: one modeldef, an instance a cell, under the folder's camera), and the level's clock is stopped while the folder is up, so the page lends a screen the frame's own; the large view is the TV's own screen node drawn alone through `tvscreenRender()` (it was a second renderer and disagreed with the sets: no mip levels, no alpha, no turn), `geMonitorImage()` answers only a screen running one of GoldenEye's lists (the Institute's own TVs, drawn under the folder, were loading Perfect Dark's textures into GoldenEye's picture table), and a model on a folder page is drawn with `G_ASPECT_CENTER_EXT` off or a wide window squeezes it (`frontTvCamera()`; `monsweep.gdb` captures all 52 both ways); the folder never opens by itself headlessly - reach it through a finished `--cinema-ending`
- **A converted mission's character rows, and testing the way a tester runs** — ge-bean.md, "Three crash reports, two faults": `g_HeadsAndBodies` past the stock table is laid out in `gebean.h` - **152-175 are kept for a converted mission's bodies** (a body's row must fit a byte) and the Combat Simulator's pool starts at 176, because GoldenEye X's borrowed characters are 106 rows and used to start at 152 too, which left a mission none; a spawn command left with GoldenEye's own number spawned Perfect Dark's row of that number, and its 12 and 4 are **heads** (`Cheaddark_frockZ`, `Cheaddark_combatZ`) - no root matrix, and `chrTestHit()` died on the first frame Baron Samedi was on screen; `modelSetMatrices()` tries **`modelasm00018680()` before `model.c`**, so a guard in one twin is no guard (Runway's still plane, in the opening shot); `build/gexrom/runall_t.sh` sweeps the missions with GoldenEye X installed and `XblaGoldenEye=1`, which the plain sweep never did; and a side-by-side binary must not be named `<exe>.new`, which the updater deletes at the next start
- **GoldenEye's own 2-D collision on a converted level** — ge-bean.md, "GoldenEye's own collision for a body: the tile graph" (`port/src/gestan.c`, converter 38): GoldenEye's collision is its tile graph and nothing else, so the only walls that exist for a body are the unlinked edges of tiles *linked to the one it stands on*, within its reach; the conversion keeps the graph (`bg_gx<level>_stan`, a link being an address - `first tile - 0x80 + (link << 3)` - and not an edge number) and the collision library's four background walkers ask `geStanWallSkipped()` whether a wall's tile is linked to the body's, for queries carrying `GEOFLAG_WALL` only; which tile a wall came from is the order the conversion wrote them in, checked point for point at the load; it is what let a player up Dam's first guard tower; and a walk probe must steer round corners, arrive within a body's radius and leave characters out, or it reports the probe's own faults
- **A converted stair's rail and the wall beside it** — ge-bean.md, "Through the rail and into the tower's wall on Dam's outside stair" (converter 50): the tile a body stands on was the highest under `manground + 40`, and a player's ground lags a staircase by more than that when running up it with the stick to one side - the tile taken was the ground a storey below and every wall of the stair was left out (`stanTileUnder()`'s rise, only where the limit is a foot); and GoldenEye links a tread to a deck 317 over it through tiles that stand on edge, which is a drop from the top and a walk into the wall from the bottom (its only check is an edge 175 over Bond's eye), so the conversion raises a wall on the low side of **a link that climbs** more than 60 (`stanClimb()`, bit 14 of the link in the graph file; a ladder's link is left alone); `build/gexrom/realwalk.py` walks with the game's own walking and is the probe that reproduces a "stuck on the stairs" report - a move-function probe does not slide
- **A converted level's ladders** — ge-bean.md, "Dam's ladders: up was fine, down was a fall" (converter 51): Perfect Dark only takes hold of a ladder that reaches over the player's feet and GoldenEye's end level with the floor at their head, so every converted ladder climbed and none could be climbed down; **raising the head in the conversion does not work** (a ladder's normal is turned to face the player, so from behind the head is climbed and fallen off) - on a remake stage `bwalkUpdateVertical()` takes the ladder from the top where the second ladder test finds it and the ground is 30 below the feet, and it keeps its hold while the player keeps walking; an upright tile touching a ladder gets no climb wall
- **Anything that rolls the weapon table on a converted level** — ge-bean.md, "Start Armed's Random handed out a Mauler on Dam": `mpWeaponRowSuitsStage()` keeps a roll to GoldenEye's rows on a remake stage unless "GE Plus: Include Perfect Dark Guns" is on (one switch for the sets and the rolls), falling back to every row when GoldenEye's are all switched off - which they are in `build/gexrom`'s pd.ini (`XblaGoldenEye=0`), so set it before testing anything about GoldenEye's guns there
- **The room a converted level's picture is drawn from** — ge-bean.md, "Sky round a stairwell: the room a picture is drawn from": GoldenEye does not draw from the tile's room - it starts there and follows a plumb line from the floor under the eye up to the eye, changing room at every portal that is not upright (`bgRoomVisibilityRelated()`), so a player two steps down a stairwell, standing on the stair's tiles with their eye over the deck, is drawn from the deck's room; without it the deck was reached through a doorway a storey down and drawn in that doorway's box, and the sky showed round the stairwell (`geRoomCamera()`, first person only; the prop stays in the tile's room); and a gdb Python `stop()` must never make an inferior call
- **A converted level's portals are slabs** — ge-bean.md, "A portal is a slab: GoldenEye's thickness and its special portals" (converter 52): GoldenEye's portal record has a thickness byte (mantissa in quarters, doubled by an exponent) inside which the camera is in both rooms and the room beyond gets the whole screen, and a SPECIAL flag that gives the whole screen once the portal is in view - set in the file on Dam and Jungle and **by a table in the code** on Control and Jungle (`specialportalarray`); the conversion carries both (thickness re-coded in world units in the record's spare byte, the flag as `PORTALFLAG_02`) and bg.c follows GoldenEye's three rules behind `g_BgGePortals`, on a remake stage only; it is what "in the void until you keep stepping" was - a step past a doorway's plane with the floor still the first room's drew nothing but sky; **never judge a visibility change by pixel-diffing two runs** (more rooms on screen changes which guards tick as on screen, and the simulation diverges) - toggle inside one run (`portalab.py`, `padab.py`)
- **A converted mission's switches and its truck's heading** — ge-bean.md, "Dam's truck drove sideways, and its gates had no switches" (converter 46): GoldenEye's `PROPDEF_SWITCH` (0x13, which the decompilation's setups mislabel `LinkProps`) is how a console opens a gate and was converted as nothing; Perfect Dark's `OBJTYPE_LINKLIFTDOOR` is the same record a word longer and `doorCallLift()`'s "unused" door-to-door branch is GoldenEye's behaviour, so it goes through as a short record; Dam's gates are an **airlock pair** and a guard shuts the first behind the truck at ~frame 259, which cancels a twin's wait - press a console after frame 600 or the probe reports GoldenEye's own behaviour as a fault; a vehicle's heading is not in its record - GoldenEye takes it on the first tick from the path or the placed matrix (`PROPFLAG_INMOTION` = `OBJFLAG_CHOPPER_INIT`) and rebuilds the matrix from the heading times the scale read off `realrot`
- **GoldenEye's gadgets, the dive off Dam, and rooms named by a pad** — ge-bean.md, "Dam's dive, and the covert modem" and "The rest of GoldenEye's gadgets, and its own models in the hand" (`port/src/gegadgets.c`, converters 47-48): **Perfect Dark reads a "pad's room" argument as a room number unless it is pad + 10000** (`chrGetPadRoom()`), which meant no room trigger (264 `IFBondInRoomWithPad`) and no enter-room or deposit-in-room objective had ever fired on a converted mission; **a background chr's target is prop 0**, not the player; GoldenEye's thrown gadgets are mines to its own code (ECM mine hosts), six gadgets have no model in the hand and are only ever "equipped while the object is activated" (Data Uplink hosts, `WEAPONFLAG_FIRETOACTIVATE`), **a weapon number is an s8 so eleven items share seven numbers, two of them by mission**; the photograph check must run in the render (it reads prop matrices); `port/src/preprocess/filesetup.c` drops any record field it does not name (a throw-in-room record's weapon arrived as zero); and the in-hand model is measured and placed, because GoldenEye poses it with a hand animation and its WeaponStats position places nothing
- **A long fall on a converted mission** — ge-bean.md, "The dive off Dam killed Bond": Perfect Dark kills a player four seconds into a fall (`TICKS(240)` in `bwalkUpdateVertical()`, forced, so invincibility does not stop it) and GoldenEye has no such clock - Dam's ending is 350 ticks of falling; off on a converted level's mission, kept in its arenas; the first dive probe's log showed the death and was read as a pass, so a mission-ending probe prints `isdead`
- **Judging a conversion against GoldenEye itself** — ge-bean.md, "Dam against the oracle": the native GoldenEye port on `10.8.0.3` walks into solo Dam on a pad script and gdb warps Bond to every fifth waypoint's pad (`~/dam-oracle/getour.py`), ours does the same through `padUnpack()` (`build/gexrom/padtour.py`) - a converted level is GoldenEye's runtime world plus an offset, pad numbers and `vv_theta` mean the same in both, so pad N heading H is the same picture (164 pairs in `build/gexrom/tour/cmp*.jpg`); it found that **a GoldenEye path is a list of waypoints and Perfect Dark's of pads** (every patrol and the truck's route, all twenty missions), that the truck rode under the road (the "green slab"), that **Dam and the two Surfaces are drawn at a fifth of their size** so their fog row's distances are too, that the bg file's **visibility script was dropped** (Dam's cliffs are rooms no portal reaches; seven levels carry one) and that the intro's weapon was an item id (Bond's MagSec); and **on a remake stage the room is the floor tile's** (`port/src/geroom.c`), GE Plus lists GoldenEye's own fourteen weapon sets out of the ROM with Perfect Dark's behind `Mod.GePlusPdGuns`, and every GoldenEye gun reloads by lowering; the tower stairs are still blocked and the user's proposal - GoldenEye's own 2-D tile collision for bodies - is the way to end that class
- **A converted GoldenEye level's floor vanishing under the player** — ge-bean.md, "Falling through a converted level's floor": GoldenEye's stan and its portals disagree about which room a place is in and Perfect Dark believes the portals, so where the floor's room is not one of the player's there is no floor at all and `bwalkUpdateVertical()` drops them out of the world (a strip of Runway's snow eight hundred wide is room 14's while a walker there is in room 13); asked again from `bgFindRoomsByPos()` where the first answer was nothing, on a remake stage only. The note also carries the gdb probes - the pure ground query rather than a teleport sweep, which is contaminated by the fall's own velocity - and what an F3 trace now says about which map a mod stage is

- **The Friends of Joanna collab tree, `../pd-fojo-monorepo-collab/`** — [fojo-collab.md](CLAUDE-notes/fojo-collab.md): what their mod loader does that ours does not, why the trees cannot merge, and what is worth borrowing
- **Measuring a crowded match, `--rng-seed`/`--fixed-step`/`--exit-frame`, where the frame goes** — [performance.md](CLAUDE-notes/performance.md): compare instructions per frame on a seeded fixed-step match; the renderer is 60% of the main thread and is built at -O2; why the decomp at -O2 played a different game (game-defined sinf/cosf, an uninitialised pad flag) and how a divergence is bisected; run to a level frame rather than a wall-clock span, and check a renderer change with a pixel diff; why Model LOD does nothing
- **The third person camera, and why melee, rockets and beams came out of it** — [third-person.md](CLAUDE-notes/third-person.md): the player's shot is fired from the camera and not the eye, so the crosshair is honest at any offset but everything that measures *from* the origin was two metres out; the two halves of the fix and the melee site that does not go through the shot path; the floor flags the camera trace needs; driving the camera from gdb; **Camera Tether** (`Mod.ThirdPersonTether`), a rod that pivots about `bond2.unk10` and not the shaken copy, and the game's `atan2f()` answering in 0 to tau; light glares are depth-less sprites, drawn before the gun and occluded by the body through `shotTestLos()`, and **Glare Clipping** (`Mod.GlareClip`) depth-tests their halos at draw time (`G_SETRECTDEPTH_EXT`), since the check misses props, is a frame or two stale, and passes a whole halo for a sliver of light; the player's footsteps are `bmoveTick()`'s alone, since the animated body's `footstepCheckDefault()` read the head bob's frame out of `oldframe` and played them in bursts
- **GE Plus in third person, and a converted pane's tail** — ge-bean.md, "Third person is broken sometimes" (converter 49): the solo converter carried no tail for tinted glass, so every pane was opaque at any distance and shut **portal 0** of its level every frame (Aztec's start doorway: nothing drawn from a camera beyond it), and the opening swirl's fade-out of Bond's body stays on the chr, so third person after a fully watched opening drew him at alpha 0; third-person.md, "The player's own tracers": the hand beams are drawn by `propsRenderBeams()` in third person and start at the body's gun
- **Defaults and the Settings Preset** — `src/game/modoptions.c` and `g_ModPresets` in `port/src/optionsmenu.c`: since 2026-09-09 a fresh pd.ini is stock Perfect Dark with the fixes on and the additions off (and since 2026-09-13 Smooth Text and Thin Text Outlines - "Clean Text Outlines" until 2026-09-12, and still that in pd.ini and in the code - are off too, in the defaults and in the Vanilla and Ghost Trials presets, so the text is the ROM's), because a tester met a game that jumped, rolled, tilted and flew (F was the Spectator); the fork's old defaults are the "Dab's Settings" preset, and a preset is applied on selection and remembered by nothing - the dropdown reads Custom once any covered setting differs. The third person camera has its own preset and is not covered; a pd.ini written by an older build keeps every value it has, including F
- **Weapon numbers, `flags2`, converting a `weaponnum` comparison** — [weapons.md](CLAUDE-notes/weapons.md): the four checks, and what is deliberately not converted; a launcher branch keyed on the number must test the function's type, or a mod's table crashes it; and **weapons past the stock table** (2026-09-15, GoldenEye's guns 0x5e-0x76): every switch or `==` on a weapon number asks `weaponHost()`, range tests were decided one by one because some guard arrays indexed by the real number, and the check that stock is unchanged is a seeded match compared pixel for pixel
- **The Randomizer: a mission dealt again from its own pieces** — [randomizer.md](CLAUDE-notes/randomizer.md): the roll goes between `setupLoadFiles()`'s writable copy and `setupCreateProps()`'s walk; `chrGetPadRoom()` hands a pad number back as a room; the intro stream's commands are 8, 12, 16, 32 and 40 bytes and have no length function; rewriting the spawn pad does not move a mission that opens with a cutscene; a tagged object survives being picked up, which is what makes a generated collect objective finishable; each decision draws from its own stream so a change to one part cannot move the others, and the log's fold is how that is checked; Endless Mode keeps dealing objectives and scores rooms covered; the Carrington Institute is a level and must be excluded from anything that runs on one; Chicago (0x1d) is the test bed and Villa never reaches gameplay under `--boot-stage`; and **a pad is not automatically somewhere a player can stand** - the game's ground search takes the highest floor *strictly below* the position it is handed and answers `-4294967296` for none at all, which the spawn then stands the player on, so 2% to 8% of a stage's pads are an out-of-bounds death (`modRandomPadSpawnPos()`, v3); and a level's **forced walk** (the Duel's, Extraction's) only ends on arrival, so from a dealt start it ran into a wall for ever - `modRandomTickForcedWalk()` ends one that stops closing in
- **The Randomizer's run: a room at a time across every map** — [randomizer-run.md](CLAUDE-notes/randomizer-run.md): every door out of the landing room is a portal and a portal is a whole stage load (the co-operative Deep Sea chain is the pattern, and `mainChangeToStage()` takes effect at the *end* of the frame); a mission's opening cutscene held the first landing off for 45 seconds; the kit goes back through the spawn (inventory inside `playerStartNewLife()`, hands from `playerSpawnWeapons()`, health after `playerSpawn()`) and never from the tick, which left a hand drawing a model nothing had loaded; the door test is "not in `prop->rooms[]`", not "`rooms[0]` changed"; a death must not end the stage; no objective type fits one room, so the run owns the top bit of `g_StageFlags`; the room is sealed until that objective is done and the doorway is a wall from the inside, and what is sealed is the landing room **plus the rooms touching it** (`modRunBuildZone()`, one door deep, built at the landing and dropped back to the one room when it would swallow a whole small map) since a single room is sometimes a stairwell (a refused move must leave a collision behind it, or the push reads the last real one and a door damages the player through a wall that is not there), the barrier's test is the portal's own so the two cannot disagree, and a room sealed too long is dealt a clock; landings are waypoints in rooms that have a portal; guards are modalarm.c with the body overridden, and a body drawn from the whole game has to be spawned the way a setup spawns one (`bodyInitSpecialChr()`: the robot's fireslots, or the beam render dereferences them); `--random-run` and `--run-autohop N` drive it headlessly; the landing pad is checked for a floor before it is dealt, a sealed room's guards are dealt **into the zone** (the alarm's eight metre minimum is most of one, so every guard used to start outside it and some could never walk in) and a kill objective nothing has reached for forty seconds is re-dealt as a clock, and the doors out of the zone lose their key flags when the seal lifts; a room won is a room to rest in since 2026-09-12 - full health and shield, the hostiles in the zone killed and no more dealt until the next landing (modRunGetGuardCount() answering 0), and the guards halved to six up and six per ten seconds, both halves because the cap alone is answered by the rate; chrDamage() takes neither a NULL vector nor a NULL gset; a landing on a Stage Loader map froze the game outright, because a mod stage row carried the template's solo setup and Skedar's pad 99 over a 98 pad map unpacked room -256, which the collision walk indexed g_TileRooms with and then walked forever on a geo type with no length; and the room's objective is the whole objective list, so a stage script's "all objectives complete" ended the mission on it and Next Mission loaded a stock mission dealt as a landing - a run's map never ends by its script (`aiIfAllObjectivesComplete`, `aiEndLevel`), the endscreen ends the run (`mainEndStage()`), and a stage the run did not ask for stops it before the roll (`modRunCheckLoad()`)
- **A rule or colour a mod's code changes that is not a weapon's** — mods.md, "The tail": `game/modrules.h` holds it with the stock default, a modconfig block sets it through one setter in mod.c, both importers read it; the branch `lua-pipeline` is the Lua experiment of 2026-09-07, kept and not merged. mods.md, "A GE-X tester's seven reports" (2026-09-13, importer 32): a jump table in rodata is *data* to modcodediff (the third person guns, the sights); GE-X's green menus are dialog **type bytes**, not the palette; a boot re-imports every stale mod, so never run two after a version bump
- **Projects we can do, not started** — [project-list.md](CLAUDE-notes/project-list.md): work the user has judged worth doing, with what is already known; first entry (2026-09-14) is rendering the scene more than once a frame for real reflections, where `gfx_run` is 59% of the game thread and building the lists ~3%, so the extra passes must be made cheap first (measure the rooms/chrs split, rooms on the GPU, posed models under one more matrix), and a render-only pass must not rerun the visibility flags the AI reads

**[DabDavisGitHub.md](DabDavisGitHub.md)** is the companion to this file: the
GitHub remote, how commits are written, how a push becomes a release, and how the
stable and dev channels reach a player. Read it before pushing or tagging —
`dabs-mod` is a public default branch and a push to it rebuilds what every
dev-channel player's Check for Updates points at. A push that changes only
`CLAUDE.md`, `CLAUDE-notes/` or `DabDavisGitHub.md` does not build.

When a session gets something wrong that the code could not have told it, write
it down: a new note, or a section in the one for its area, and a line here.

## GE-X import: where to pick up (2026-09-05)

The console mod GE-X 6a is the reference case for the mod loader. Its assets,
data tables, missions, music, environments, star field, weather, shield
colours, hit sounds, co-op buddies, the weapon lists behind eighteen flags, two damage rules and the unlocks all import (`build/mods/GE-X_6a_01-19-25/`, importer version 23); what is left is the code
GE-X *rewrote*, which `modcodediff` lists and nothing follows yet. Read
[mods.md](CLAUDE-notes/mods.md) from "GE-X's solo missions in the port" to the
end before touching any of it, then:

1. **Regenerate the list** — the summary is the work queue, largest first:
   ```sh
   python3 tools/modcodediff --rom ../pd-upstream/pd.ntsc-final.z64 \
       --patch build/mods/GE-X_6a_01-19-25/GE-X_6a_01-19-25.xdelta --summary
   ```
   `constants` regions are tables to follow (most are done); `rewritten` ones
   need reading. Every rewritten function of 9 words or more is read, and
   the tail's unlocks family too; what remains of the tail is listed in
   mods.md ("The tail, and the unlocks": the menu palette, the run-speed
   cave, King of the Hill, GE-X's hats ...), and the weapon-number sites
   the port still tests literally (weapons.md: ~60 in bondgun.c, ~63 in
   propobj.c), each a `FLAG_SITES` row once converted. GE-X's own
   weapon-number regions are all read as of importer 27 (mods.md, the
   four "number sites" sections; the second says why a row also needs a
   `codeSyms[]` entry in modimport.c), and five of its tail as of 29
   (mods.md, "The tail": settings in `game/modrules.h`, and what is left).
   `--prepare-diff DIR` writes both binaries; `mips-linux-gnu-objdump -b binary
   -m mips:4300 -EB -D --adjust-vma=0x7f000000` reads them.
2. **Two ways to follow a change.** A renumbered compare is a
   `follow_immediate(s)` site (the `playerconst`/`bgstage` pattern, one table
   row in both importers). A rewritten function is run on the toy MIPS
   (`emulate()` in `tools/importmod`, `emuRunArgs()` in `port/src/modimport.c`,
   the weather is the worked example, the shield colour the one for a
   function that tests its caller, the hit sounds the one for a list of
   weapon numbers: seed the tables, run per number, read what it stored)
   and written out as the port's own config. A weapon-number test in the
   port becomes a flag (weapons.md) and the importer writes `weaponflags`;
   arguments to a call are followed by call and register
   (`follow_call_args()`, the co-op buddies), since one function can hold
   four number spaces; a weapon test that became a list is read as a
   compare chain (`follow_compare_chain()`, `FLAG_SITES`), and a flag is
   written only when every site of it agrees. Across the archive the most rewritten functions are
   `player_tick` (38 mods, 27 of them a no-op word - see mods.md), then a
   block of 23 that every "all solos in multi" patch shares (`tex_init`,
   `setup_create_props`, `mp_start_match`, the unlock handlers);
   `modcodediff --summary` over every patch takes four minutes with `xargs
   -P 8` and is how to know whether a function is one mod's or the
   archive's. Whatever is added goes in **both importers**, `IMPORT.txt`
   lines identical, and bumps `MODIMPORT_VERSION` so old imports redo
   themselves.
3. **Test headlessly** on Runway (0x22) or later — Dam (0x30) and Facility
   (0x33) are unfinished in the patch and prove nothing:
   ```sh
   cd build && timeout -k 5 60 xvfb-run -a ./pd.x86_64 --moddir mods/GE-X_6a_01-19-25 \
       --savedir /tmp/pdsave --skip-intro --no-sound --boot-stage 0x22 --log
   ```
   Exit 124 is "ran until stopped"; 137 is a crash or hang, read the log.
   `--moddata-trace`, `--chr-trace`, `--setup-trace` and the
   `weather:`/`music:` log lines say what applied. `--boot-stage` takes the
   stage id, not the menu slot: GE-X's `g_SoloStages` remaps six missions.
4. **Open tester reports**: no guards on Dam (GE-X spawns them by AI as the
   player advances; a headless run cannot exercise it — a person must). Check
   the tester's binary commit before debugging a report from
   `sdg@10.8.0.3:~/pd-test/` (`strings pd.x86_64-linux | grep -m1 -E
   '^[0-9a-f]{7}$'`).
5. **Known gaps, deliberate**: stages a mod took weather away from keep the
   port's stock weather entry (inert without a rain/snow script command); the
   two importers order the `playerconst`/`bgstage`/`roomstage`/`buddyconst`
   lines differently (content identical); mod-only stage ids (GE-X's 0x4d–0x50)
   are skipped by `stage {}` blocks with a warning.

## Build and run

```sh
cmake -G"Unix Makefiles" -Bbuild .     # only after adding/removing source files
cmake --build build -j8
./build/pd.x86_64                      # needs build/data/pd.ntsc-final.z64
./build/pd-modded.sh                   # with the All in One mod
```

Release builds use `-Og` for the decomp except the hot files (`PD_HOT_O2`) — see the comment in `CMakeLists.txt` and [performance.md](CLAUDE-notes/performance.md). Warnings
about uninitialised locals in `collision.c`, `model.c`, `menu.c` and `mplayer/setup.c`
are pre-existing decomp artifacts; check `git diff` before assuming one is yours.

`build/` is gitignored, including the mod directories and `pd-modded.sh`. Keep the
mod zip: a clean rebuild takes them with it.

The Windows cross-build is in [CLAUDE-notes/windows-build.md](CLAUDE-notes/windows-build.md).

## This is a decompilation

Much of `src/` is decompiled N64 code. Two consequences that matter:

**Structs with offset comments** (`/*0x1be7*/`) document the original ROM layout.
Growing them breaks matching builds. Ours sets `MATCHING=0`, but treat it as a real
cost when considering upstreaming.

**Some code is unreachable by construction on N64** and becomes reachable the moment
a limit is raised. Every bug in this fork's history was of that shape: a fixed-size
table or an assumption nobody wrote down. `chrInit()` dereferenced NULL when the chr
pool was exhausted; `mpCreateBotFromProfile()` looped forever once chrs outnumbered
the 53 available heads; `langGetLangBankIndexFromStagenum()` had `default: while(true){}`.
When raising any cap, grep for fixed-size arrays indexed by the thing you are scaling.

## Debugging

Guessing from source failed repeatedly here; the stack was right every time.

```sh
# crash: symbolise the offsets the game prints
addr2line -f -C -e build/pd.x86_64 0x11abf5

# hang: main thread only, Mesa worker threads are noise
gdb -p $(pgrep -x pd.x86_64) -batch -ex "thread 1" -ex "bt 14"
```

**A Windows crash dialog** gives `PC` and `MAIN MODULE: [base]`, and a
backtrace of `[base]+offset` lines. The offset is from the image base, which
the release exe has at `0x140000000`, and the exe keeps its DWARF (30 MB for a
reason), so:

```sh
gh release download v3.1.2 -p pd.x86_64-windows.exe -O pd-v3.1.2.exe
x86_64-w64-mingw32-addr2line -f -C -i -e pd-v3.1.2.exe 0x1401ba7a3   # 0x140000000 + offset
```

Plain `addr2line` says `??` for every line. When the report does not say
which build, try each stable exe: the right one symbolises to a stack that
makes sense as a call chain, the wrong ones to functions that could never
have called each other. The reporter's data is not ours: a crash in a mod's
model that no stage here reproduces is a model their import has and ours does
not, and the question to ask them is which mission and what the first line of
their `mods/<mod>/IMPORT.txt` says.

stdout is block-buffered when redirected, so log lines sit unwritten. Flush a live
process before reading or killing it — `SIGKILL` discards the buffer:

```sh
gdb -p $(pgrep -x pd.x86_64) -batch -ex 'call (int)fflush(0)'
```

A hung process ignores `SIGTERM`, because the shutdown handler cannot run.

For "state disappears" bugs, instrument every mutating path rather than reading code.
The simulant-clearing hunt was solved by logging that showed a count of zero at every
suspected site — nothing was being cleared; nothing had ever been created.
